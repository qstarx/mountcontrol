GTK Control for fuser mounts
----------------------------

User can mount/unmount preconfigured
Mountpoints (fuse). Tested only with sshfs so far

Installation:

Edit fstab.sshfs.example to your needs and copy to

    /etc/fstab.sshfs
